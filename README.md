# mixture-of-depths-routers-llama
Dynamic-depth router tuning for LLaMA-1B (works with TinyLLma as well) > inject lightweight Mixture-of-Depths routers, freeze base weights, fine-tune only routing with a compute budget penalty.
